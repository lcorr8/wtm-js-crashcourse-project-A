extends layout

block content
    h1 Employers
    if employers.length
        p Here are the employers:
        table
            thead
                td ID
                td Name
            each employer in employers
                tr
                    td #{employer._id}
                    td #{employer.name}

        pre= JSON.stringify(people, null, 2)
    else
        p There's no one yet :(
    script.
        console.log(!{JSON.stringify(employers)})